
describe("call reader", function () {
    var reader;
    beforeEach(function () {
        reader = Reader();
    });
    it("should work", function () {
        // g_for_script();
        reader;
    });
});